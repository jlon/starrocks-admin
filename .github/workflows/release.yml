name: Release

on:
  push:
    tags:
      - 'v*.*.*'  # Triggers on version tags like v1.0.0, v2.1.3, etc.

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
      version: ${{ steps.get_version.outputs.version }}
    
    steps:
    - name: Get version from tag
      id: get_version
      run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

    - name: Create Release
      id: create_release
      uses: softprops/action-gh-release@v1
      with:
        name: StarRocks Admin ${{ github.ref_name }}
        draft: false
        prerelease: false
        body: |
          ## StarRocks Admin ${{ github.ref_name }}
          
          ### 🚀 What's New
          
          Please see the [CHANGELOG](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for details.
          
          ### 📦 Installation
          
          #### Quick Start (Recommended)
          ```bash
          # Download and extract
          wget https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/starrocks-admin-linux-amd64.tar.gz
          tar -xzf starrocks-admin-linux-amd64.tar.gz
          cd starrocks-admin
          
          # Start the service
          ./bin/starrocks-admin.sh start
          
          # Access the application
          open http://localhost:8080
          ```
          
          #### Docker (Multi-platform: amd64/arm64)
          ```bash
          # Pull the image (automatically built by docker-publish workflow)
          docker pull ghcr.io/${{ github.repository }}:${{ github.ref_name }}
          
          # Or use semantic version tags
          docker pull ghcr.io/${{ github.repository }}:latest
          
          # Run the container
          docker run -d -p 8080:8080 ghcr.io/${{ github.repository }}:${{ github.ref_name }}
          ```
          
          > **Note:** Docker images are built automatically by the `docker-publish` workflow and support both amd64 and arm64 architectures.
          
          ### 📋 System Requirements
          - Linux x86_64 or macOS (ARM64/x86_64)
          - 2GB RAM minimum
          - 1GB disk space
          
          ### 🔗 Links
          - [Documentation](https://github.com/${{ github.repository }}/blob/main/README.md)
          - [Installation Guide](https://github.com/${{ github.repository }}/blob/main/docs/INSTALLATION.md)
          - [Configuration Guide](https://github.com/${{ github.repository }}/blob/main/docs/CONFIGURATION.md)
        body: |
          ## StarRocks Admin ${{ github.ref_name }}
          
          ### 🚀 What's New
          
          Please see the [CHANGELOG](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for details.
          
          ### 📦 Installation
          
          #### Quick Start (Recommended)
          ```bash
          # Download and extract
          wget https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/starrocks-admin-linux-amd64.tar.gz
          tar -xzf starrocks-admin-linux-amd64.tar.gz
          cd starrocks-admin
          
          # Start the service
          ./bin/starrocks-admin.sh start
          
          # Access the application
          open http://localhost:8080
          ```
          
          #### Docker (Multi-platform: amd64/arm64)
          ```bash
          # Pull the image (automatically built by docker-publish workflow)
          docker pull ghcr.io/${{ github.repository }}:${{ github.ref_name }}
          
          # Or use semantic version tags
          docker pull ghcr.io/${{ github.repository }}:latest
          
          # Run the container
          docker run -d -p 8080:8080 ghcr.io/${{ github.repository }}:${{ github.ref_name }}
          ```
          
          > **Note:** Docker images are built automatically by the `docker-publish` workflow and support both amd64 and arm64 architectures.
          
          ### 📋 System Requirements
          - Linux x86_64 or macOS (ARM64/x86_64)
          - 2GB RAM minimum
          - 1GB disk space
          
          ### 🔗 Links
          - [Documentation](https://github.com/${{ github.repository }}/blob/main/README.md)
          - [Installation Guide](https://github.com/${{ github.repository }}/blob/main/docs/INSTALLATION.md)
          - [Configuration Guide](https://github.com/${{ github.repository }}/blob/main/docs/CONFIGURATION.md)

  build-and-upload:
    name: Build and Upload Assets
    needs: create-release
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: starrocks-admin-linux-amd64
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: starrocks-admin-macos-amd64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: starrocks-admin-macos-arm64
    
    runs-on: ${{ matrix.os }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: ${{ matrix.target }}

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Cache Rust dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/bin/
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          backend/target/
        key: ${{ runner.os }}-${{ matrix.target }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-${{ matrix.target }}-cargo-

    - name: Build application
      run: make build
      env:
        CARGO_BUILD_TARGET: ${{ matrix.target }}

    - name: Create release package
      run: |
        cd build/dist
        tar -czf ${{ matrix.artifact_name }}.tar.gz \
          --transform 's,^,starrocks-admin/,' \
          bin conf lib data logs migrations

    - name: Upload Release Asset
      uses: softprops/action-gh-release@v1
      with:
        files: build/dist/${{ matrix.artifact_name }}.tar.gz

  # Note: Docker image building is handled by docker-publish.yml workflow
  # which is automatically triggered when a tag is pushed

  publish-helm-chart:
    name: Publish Helm Chart
    needs: create-release
    permissions:
      contents: write
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install Helm
      uses: azure/setup-helm@v3
      with:
        version: 'latest'

    - name: Package Helm chart
      run: |
        cd deploy/chart
        helm package . --version ${{ needs.create-release.outputs.version }}

    - name: Upload Helm chart to release
      uses: softprops/action-gh-release@v1
      with:
        files: deploy/chart/starrocks-admin-${{ needs.create-release.outputs.version }}.tgz
